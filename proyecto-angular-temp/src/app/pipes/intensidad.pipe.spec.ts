import { IntensidadPipe } from './intensidad.pipe';

describe('IntensidadPipe', () => {
  it('create an instance', () => {
    const pipe = new IntensidadPipe();
    expect(pipe).toBeTruthy();
  });
});
